steps:
  - uses: actions/checkout@v4
  - uses: actions/setup-java@v4
    with:
      java-version: '17'
      distribution: 'amazoncorretto'
  - name: Run the Maven verify phase
    run: mvn --batch-mode --update-snapshots verify -Dspring.profiles.active=prod